services:
  - type: web
    name: optimizing-multimodal-emotion-recognition
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: START_BACKEND
        value: "1"
      - key: BACKEND_PORT
        value: "8001"
